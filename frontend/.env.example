# Public base URL that basket collection links point at (no trailing slash).
# Copy this file to .env and adjust the value as needed; .env is gitignored.
# When unset, the UI falls back to the origin it is served from.
BASKETCASE_URL=https://basketcase.com
